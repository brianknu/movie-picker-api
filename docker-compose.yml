version: "3"
services:
  recommendation-api:
    build: .
    container_name: recommendation-api
    ports:
      - 5000:5000
    networks:
      - pizzabot
    restart: unless-stopped

networks:
  pizzabot:
    external:
      name: docker_pizzabot
